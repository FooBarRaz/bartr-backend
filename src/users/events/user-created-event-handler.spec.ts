import { UserCreatedEventHandler } from './user-created-event-handler';

describe('UserCreatedEventHandler', () => {
  it('should be defined', () => {
    expect(new UserCreatedEventHandler()).toBeDefined();
  });
});
